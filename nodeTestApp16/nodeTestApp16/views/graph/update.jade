extends ../layout
include ../mixins/updateForms.jade

block content
  .top-section.mdl-typography--text-center
    .logo-font.android-sub-slogan #{slogan}:
    .mdl-typography--font-light
      br
      +relationshipUpdateForm
    script.
      document.getElementById('optionSource').textContent = '#{relationship.row[0]}';
      document.getElementById('optionType').textContent = '#{relationship.row[1]}';
      document.getElementById('optionTarget').textContent = "#{relationship.row[2]}";
      document.getElementById('property').value = '#{relationship.row[3]}';

      app.content.htmlElements.toggleViewUpdateRelationship(!{JSON.stringify(relationship)});



